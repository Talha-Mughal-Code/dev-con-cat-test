<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= [yield(:title).presence, "Super Pixel"].compact.join(" · ") %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application" %>
  </head>
  <body>
    <main class="wrap narrow" style="padding-top:64px">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>
  </body>
</html>
